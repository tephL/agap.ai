## Mobile Client of AGAP.ai >.<

### Instructions
1. `npm install`
1. Duplicate `.env.example` and rename it to `.env`
1. Declare `EXPO_PUBLIC_API_URL` for the backend's IP
    - this could be a local backend, or the url of the deployed backend
1. `npx expo start`

